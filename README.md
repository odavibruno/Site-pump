
  # Mouse-Responsive Gradient Background

  This is a code bundle for Mouse-Responsive Gradient Background. The original project is available at https://www.figma.com/design/1JNkrztazXAGsnhZkmpYQr/Mouse-Responsive-Gradient-Background.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  